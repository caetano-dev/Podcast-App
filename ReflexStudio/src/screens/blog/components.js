import React from "react";
import { View, StyleSheet } from "react-native";
import { Layout, Text, Button, Card } from "@ui-kitten/components";

export const BlogCard = ({}) => {
  return (
    <Layout style={{}}>
      <View style={{ border: "4px solid", padding: 30 }}></View>
    </Layout>
  );
};
